# TODO: Set up Terminal Mode Routing

- [x] Update App.tsx to include React Router setup with routes for "/" and "/terminal"
- [x] Update Hero.tsx to use useNavigate for smooth navigation to "/terminal"
- [x] Fix terminal background to be dark like the website
- [x] Change terminal font to typewriter and fix input text visibility
- [x] Test the terminal mode by running the dev server and clicking "Terminal Mode"
